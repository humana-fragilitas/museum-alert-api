{
  "name": "museum-alert-api",
  "version": "1.0.0",
  "description": "AWS CDK-based stack for defining and deploying cloud infrastructure and services meant to complement the 'Museum Alert' IoT project",
  "main": "index.js",
  "scripts": {
    "cdk": "cdk",
    "prepare-layer": "cd lambda/lambdaLayer/nodejs && npm install --only=production",
    "clean-layer": "rm -rf lambda/lambdaLayer/nodejs/node_modules",
    "rebuild-layer": "npm run clean-layer && npm run prepare-layer",
    "deploy:dev": "npm run prepare-layer && AWS_PROFILE=cdk-deploy cdk deploy --all --context stage=dev --require-approval never",
    "deploy:prod": "npm run prepare-layer && npm run build && AWS_PROFILE=cdk-deploy cdk deploy --all --context stage=prod --require-approval never",
    "destroy:dev": "AWS_PROFILE=cdk-deploy cdk destroy --all --context stage=dev --force",
    "destroy:prod": "AWS_PROFILE=cdk-deploy cdk destroy --all --context stage=prod --force",
    "diff:dev": "npm run prepare-layer && AWS_PROFILE=cdk-deploy cdk diff --all --context stage=dev",
    "diff:prod": "npm run prepare-layer && AWS_PROFILE=cdk-deploy cdk diff --all --context stage=prod",
    "synth:dev": "npm run prepare-layer && AWS_PROFILE=cdk-deploy cdk synth --all --context stage=dev",
    "synth:prod": "npm run prepare-layer && AWS_PROFILE=cdk-deploy cdk synth --all --context stage=prod",
    "bootstrap:dev": "AWS_PROFILE=cdk-deploy cdk bootstrap --context stage=dev",
    "bootstrap:prod": "AWS_PROFILE=cdk-deploy cdk bootstrap --context stage=prod",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/humana-fragilitas/museum-alert-api.git"
  },
  "author": "andrea@humanafragilitas.com",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/humana-fragilitas/museum-alert-api/issues"
  },
  "homepage": "https://github.com/humana-fragilitas/museum-alert-api#readme",
  "dependencies": {
    "aws-cdk-lib": "^2.208.0",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity": "^3.872.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.872.0",
    "@aws-sdk/client-dynamodb": "^3.872.0",
    "@aws-sdk/client-iot": "^3.872.0",
    "@aws-sdk/client-iot-data-plane": "^3.872.0",
    "@aws-sdk/util-dynamodb": "^3.872.0",
    "@babel/core": "^7.28.3",
    "@babel/preset-env": "^7.28.3",
    "@jest/globals": "^29.7.0",
    "@types/node": "^24.1.0",
    "aws-cdk": "^2.1023.0",
    "aws-sdk-client-mock": "^4.1.0",
    "aws-sdk-client-mock-jest": "^4.1.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jose": "^6.0.13",
    "ts-node": "^10.9.2"
  }
}
